<?php

namespace {{ projectName }}\Presentation\Request\{{ entityName }}\Query;

use Sfynx\DddBundle\Layer\Presentation\Request\Generalisation\QueryRequestInterface;
use Sfynx\DddBundle\Layer\Presentation\Request\Generalisation\AbstractRequest;

/**
 * Class NewRequest.
 *
 * @category   {{ projectName }}
 * @package    Presentation
 * @subpackage Request
 */
class SearchByRequest extends AbstractRequest implements QueryRequestInterface
{
    protected $defaults = [
        'start' => 0,
        'count' => 10,
    ];

    protected $required = [
        'criteria'
    ];

    protected $allowedTypes = [
        'criteria' => ['array'],
        'start' => ['integer'],
        'count' => ['integer'],
    ];
}
