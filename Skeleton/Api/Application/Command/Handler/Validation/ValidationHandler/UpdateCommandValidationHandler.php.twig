<?php

namespace {{ projectName }}\Application\{{ entityName }}\Command\Validation\ValidationHandler;

use Sfynx\DddBundle\Layer\Application\Generalisation\Validation\ValidationHandler\AbstractCommandValidationHandler;
use Symfony\Component\Validator\Constraints\NotBlank;

/**
 * Class UpdateCommandValidationHandler.
 *
 * @category   {{ projectName }}
 * @package    Application
 * @subpackage Command\Handler\Validation\ValidationHandler
 */
class UpdateCommandValidationHandler extends AbstractCommandValidationHandler
{
    /**
     * Init the constraints the updating entity properties must validate
     *
     * @return $this
     */
    protected function initConstraints()
    {
{% if entityFields %}
        $this{% for field in entityFields %}->add('{{ field.name }}', new NotBlank()){% if loop.index < entityFields|length %}

            {% else %};{% endif %}{% endfor %}
{% endif %}

        return $this;
    }
}
